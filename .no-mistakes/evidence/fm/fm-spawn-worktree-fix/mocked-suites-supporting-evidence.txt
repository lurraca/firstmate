Mocked-transport suites run on final head 62262ae (SUPPORTING evidence only;
the LIVE scenarios driven against real tmux+treehouse are the primary evidence
in live-spawn-AF.txt, live-relaunch-G.txt, live-dirid-B.txt, live-deadline-E.txt).

These suites use a fake tmux/perl/treehouse to drive the failure-mode paths that
real tmux cannot reproduce (clean send failure then recovery; unsafe partial
input status 2; synthetic monotonic clock that injects probe/wait time). They are
reported as untested-LIVE with that reason, not as passes, per the user intent.

== tests/fm-spawn-worktree-settle.test.sh ==
ok - a single transient stale pane_current_path read is not accepted as the worktree
ok - an already-settled pane confirms on the next read, not a whole extra cycle
ok - a transient primary-checkout pane read is not accepted as the worktree
ok - a pane stuck on the primary checkout fails loudly at the deadline
ok - a swallowed first delivery is retried with one idempotent treehouse allocation token
ok - a clean delivery failure retries and only the submitted line counts as accepted
ok - unsafe partial input aborts before another command can be appended
ok - bounded clean delivery failures retain a useful final diagnostic
ok - treehouse-get retries stop at the configured attempt bound and retain the startup timeout
ok - one monotonic deadline charges both backend probe time and bounded sleeps
ok - retry settings accept normalized boundaries and reject malformed or overflowing values
ok - the worker launch line removes the treehouse allocation token
ok - case-insensitive spelling aliases remain the primary directory until a real worktree appears
# all fm-spawn-worktree-settle tests passed

== tests/fm-control-relaunch.test.sh ==
ok - fm-spawn --relaunch: a pane that starts outside the worktree but reaches it succeeds
ok - fm-spawn --relaunch: refuses to launch a second agent into a live endpoint
ok - fm-spawn --relaunch: refuses to start a replacement outside the copy holding its work
ok - fm-spawn --relaunch: every identity axis comes from the record, and a contradicting flag refuses
ok - fm-spawn --relaunch: an unrecorded task is refused
ok - fm-spawn --relaunch: symlinked records refuse before inspection
ok - fm-spawn --relaunch: keeps its early meta lock continuous
ok - fm-spawn --relaunch: pending closes refuse before replacement begins
ok - fm-spawn --relaunch: prepublication abort removes replacement state
ok - fm-spawn --relaunch: direct entry participates in lifecycle serialization
(+ 10 more fm-control/fm-promote relaunch transaction checks)
all fm-control-relaunch tests passed
